info:
  name: List Requests
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8000/requests
  params:
    - name: status
      value: RUNNING
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
