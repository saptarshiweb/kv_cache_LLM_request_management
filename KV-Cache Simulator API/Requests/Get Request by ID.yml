info:
  name: Get Request by ID
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8000/requests/{{request_id}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
