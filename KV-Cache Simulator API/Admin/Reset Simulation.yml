info:
  name: Reset Simulation
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8000/admin/reset
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
