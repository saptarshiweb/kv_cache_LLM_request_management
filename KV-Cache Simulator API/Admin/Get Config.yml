info:
  name: Get Config
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8000/admin/config
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
