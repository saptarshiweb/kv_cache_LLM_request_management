info:
  name: Get Metrics
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8000/metrics?last_n=100
  params:
    - name: last_n
      value: "100"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
